import {styles} from "../css/styles.js";

function Home() {
    return <div style={{...styles.divStyle}}>
        <p style={{...styles.pHome}}>Page d'accueil</p>
    </div>
}

export default Home;